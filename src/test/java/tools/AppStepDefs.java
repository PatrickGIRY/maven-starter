package tools;

import io.cucumber.java8.En;

public class AppStepDefs implements En {
    public AppStepDefs() {
        Given("a context", () -> {
        });

        When("an action", () -> {
        });

        Then("a result", () -> {
        });
    }
}
